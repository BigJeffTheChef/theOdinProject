import Player from './PlayerObj.js';

let player = new Player();
beforeEach(() => {

});

describe('players take turns', () => {
  test('init', ()=> expect(1).toBe(1));
  test('players take turns', () => {
    
  });
});
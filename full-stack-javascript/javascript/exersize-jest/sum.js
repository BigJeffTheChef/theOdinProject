function sum(num1, num2) {
  // if (typeof num1 !== 'number') {
  //   throw new Error('num1 parameter was not a number');
  // } 
  // if (typeof num2 !== 'number') {
  //   throw new Error('num2 parameter was not a number');
  // } 
  return parseInt(num1) + parseInt(num2);
}

export {sum};